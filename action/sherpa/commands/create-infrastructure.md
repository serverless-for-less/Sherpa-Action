---
description: CRUDs infrastructure based on the deployment plan.
---

Look at the `.sherpa.sh/deployment-plan.md` and other files in that folder to determine what infrastructure is needed and with what provider. Use your skills for `manage-infrastructure` to CRUD the needed infrastructure using the provider's MCP server.
Be sure you check if the infrastructure already exists first before creating.